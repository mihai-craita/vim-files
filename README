Installation:

   git clone https://github.com/mihai-craita/vim-files.git ~/.vim
   git clone https://github.com/gmarik/vundle.git ~/.vim/bundle/vundle

Create symbolic links:

   ln -s ~/.vim/vimrc ~/.vimrc

Fetching vimscripts:

   Call :BundleInstall in VIM
